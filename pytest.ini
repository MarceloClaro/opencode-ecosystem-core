[pytest]
pythonpath = .
testpaths = tests
markers =
    external: depende de infraestrutura externa ao checkout (systemd, /opt, caminhos de deploy ou portas locais); opt-in com OPENCODE_RUN_EXTERNAL_TESTS=1
