# SPEC-935-R209 — Integração LiteRT-LM ao OpenCode Ecosystem: CLI Multi-Modelo, Chat Interativo e Servidor OpenAI

**Versão**: 1.0.0  
**Status**: Proposta  
**Data**: 2026-07-21  
**Autor**: Marcelo Claro (OpenCode Ecosystem Core)  
**Rótulos**: `litert-lm`, `on-device-llm`, `cli`, `chat`, `openai-server`, `edge-ai`, `google-ai-edge`

---

## 1. Visão Geral

Este ciclo especifica a **integração do LiteRT-LM v0.14** (Google AI Edge) ao ecossistema OpenCode como uma skill completa, expondo:

1. **CLI Multi-Modelo** — comando `/litert-lm` que gerencia download, listagem, execução e servidor de modelos `.litertlm`
2. **Chat Interativo** — interface conversacional com streaming, histórico, tool use e multimodalidade
3. **Servidor OpenAI-Compatible** — wrapper para o `litert-lm serve` ou implementação própria via FastAPI
4. **Gerenciamento de Modelos** — download do HuggingFace, cache local, inspeção de metadados

### 1.1 Arquitetura

```
┌──────────────────────────────────────────────────────────────┐
│  /litert-lm (CLI)                                            │
│  run / chat / list / import / serve / info / delete          │
├──────────────────────────────────────────────────────────────┤
│  LiteRTLMSkill                                               │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────────┐  │
│  │ ModelManager │  │ ChatSession  │  │ OpenAIServer       │  │
│  │ · list       │  │ · streaming  │  │ · /v1/chat/complet │  │
│  │ · import     │  │ · tools      │  │ · /v1/models       │  │
│  │ · inspect    │  │ · multimodal │  │ · streaming        │  │
│  └─────────────┘  └──────────────┘  └────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  litert_lm (Google AI Edge SDK v0.14)                       │
│  Engine → Conversation / Session                             │
├──────────────────────────────────────────────────────────────┤
│  LiteRT Runtime (C++)                                        │
│  GPU / CPU / NPU Backends                                   │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 Modelos Suportados

| Família | Exemplo HF | Tamanhos |
|:--------|:-----------|:---------|
| Gemma 4 | `litert-community/gemma-4-E2B-it-litert-lm` | 2B, 9B, 12B, 27B |
| Gemma 3 | `litert-community/gemma-3-4b-it-litert-lm` | 1B, 4B, 12B, 27B |
| Gemma 2 | `litert-community/gemma-2-2b-it-litert-lm` | 2B, 9B, 27B |
| Llama 4 | `litert-community/llama-4-17b-it-litert-lm` | 17B |
| Llama 3 | `litert-community/llama-3-8b-it-litert-lm` | 8B, 70B |
| Phi-4 | `litert-community/phi-4-14b-it-litert-lm` | 14B |
| Qwen 2.5 | `litert-community/qwen-2.5-7b-it-litert-lm` | 7B, 32B |

---

## 2. Comandos da CLI

| Comando | Descrição |
|:--------|:----------|
| `litert-lm list` | Lista modelos disponíveis localmente |
| `litert-lm run <modelo>` | Executa modelo com prompt único e sai |
| `litert-lm chat <modelo>` | Modo interativo (REPL com streaming) |
| `litert-lm import <hf-repo>` | Download do HuggingFace |
| `litert-lm serve <modelo>` | Inicia servidor OpenAI-compatible |
| `litert-lm info <modelo>` | Inspeciona metadados do modelo |
| `litert-lm delete <modelo>` | Remove modelo do cache local |

### 2.1 Argumentos Compartilhados

| Flag | Padrão | Descrição |
|:-----|:-------|:----------|
| `--backend` | `cpu` | Backend de inferência (`cpu`, `gpu`, `npu`) |
| `--temperature` | `0.7` | Temperatura de amostragem |
| `--top-k` | `40` | Top-K para amostragem |
| `--top-p` | `0.95` | Top-P para nucleus sampling |
| `--max-tokens` | `1024` | Máximo de tokens de saída |
| `--seed` | `None` | Seed para reprodutibilidade |
| `--cache` | `disk` | Modo de cache (`none`, `memory`, `disk`) |
| `--verbose` | `False` | Logs detalhados |

---

## 3. Chat Interativo

O modo chat (`/litert-lm chat`) oferece:

- **Streaming de tokens** em tempo real com cor (amarelo para modelo, azul para canais de pensamento)
- **Tool use / Function calling** via presets Python
- **Multimodal**: attachments de imagem e áudio
- **Histórico persistente** (`~/.litert-lm/history`)
- **Atalhos**: Enter=submit, Ctrl+J=newline, Ctrl+C=clear/exit
- **Indicador de canal** para pensamento/reasoning do modelo

### 3.1 Presets

Presets são arquivos Python que definem ferramentas e instruções de sistema:

```python
# presets/assistente_pessoal.py
system_instruction = "Você é um assistente pessoal prestativo."

def get_weather(location: str) -> str:
    """Obtém a temperatura atual para uma localidade."""
    return f"25°C em {location}"
```

---

## 4. Servidor OpenAI-Compatible

O comando `/litert-lm serve` inicia um servidor HTTP com API compatível com OpenAI:

| Endpoint | Método | Descrição |
|:---------|:-------|:----------|
| `/v1/models` | GET | Lista modelos carregados |
| `/v1/chat/completions` | POST | Geração de texto (streaming opcional) |

### 4.1 Flags do Servidor

| Flag | Padrão | Descrição |
|:-----|:-------|:----------|
| `--host` | `0.0.0.0` | Host do servidor |
| `--port` | `9379` | Porta do servidor |
| `--cors-origin` | `*` | Origens CORS permitidas |

---

## 5. Critérios de Aceitação (SDD Gate)

1. **CA1**: `litert-lm list` lista modelos disponíveis sem erros
2. **CA2**: `litert-lm run` executa prompt único com streaming
3. **CA3**: `litert-lm chat` abre REPL interativo com histórico
4. **CA4**: `litert-lm import` baixa modelo do HuggingFace quando `litert_lm` disponível
5. **CA5**: `litert-lm info` exibe metadados do `.litertlm`
6. **CA6**: `litert-lm serve` inicia servidor na porta especificada
7. **CA7**: Chat interativo gerencia Ctrl+C, nova linha, submit corretamente
8. **CA8**: Presets com tool use carregam functions e system_instruction
9. **CA9**: Argumentos compartilhados (`--backend`, `--temperature`, etc.) propagam corretamente
10. **CA10**: Erros de modelo não encontrado produzem mensagem clara (exit code ≠ 0)
11. **CA11**: Todas as funções da skill são testadas com `unittest.mock` quando `litert_lm` não está compilado
12. **CA12**: Cobertura de testes ≥ 85%

---

## 6. Componentes do Sistema

### 6.1 `skills/litert_lm/skill.py` — LiteRTLMSkill

Classe orquestradora que expõe:

```python
class LiteRTLMSkill:
    def list_models() -> List[ModelInfo]
    def run(model: str, prompt: str, **kwargs) -> str
    def chat(model: str, **kwargs) -> ChatSession
    def import_model(hf_repo: str) -> ModelInfo
    def serve(model: str, host: str, port: int)
    def inspect(model: str) -> ModelMetadata
    def delete_model(model: str) -> bool
```

### 6.2 `skills/litert_lm/model_manager.py` — ModelManager

Gerencia o diretório `~/.litert-lm/models/`:

```python
class ModelManager:
    DEFAULT_DIR = "~/.litert-lm/models"
    def list_models() -> List[ModelInfo]
    def find_model(ref: str) -> Optional[ModelInfo]
    def import_from_hf(repo: str, filename: str, token: str) -> ModelInfo
    def delete_model(model_id: str) -> bool
    def inspect(model_path: str) -> ModelMetadata
```

### 6.3 `skills/litert_lm/chat.py` — ChatSession

Interface conversacional:

```python
class ChatSession:
    def __init__(engine, preset=None, sampler_config=None)
    def send(message: str, attachments: List[str]) -> Iterator[dict]
    def close()
    def cancel()
```

---

## 7. Tratamento de Erros

| Condição | Comportamento |
|:---------|:--------------|
| Modelo não encontrado | `ModelNotFoundError` + stderr + exit code 1 |
| Falha no Engine | `EngineError` + stacktrace enxuto |
| Download interrompido | `KeyboardInterrupt` limpo, sem dump |
| Arquivo inválido | `ValueError` com detalhe do parsing |
| litert_lm não instalado | Fallback para mock com aviso |

---

## 8. Referências

- [LiteRT-LM CLI Guide](https://ai.google.dev/edge/litert-lm/cli)
- [LiteRT-LM Python API](https://ai.google.dev/edge/litert-lm/api/python)
- [Modelos no HuggingFace](https://huggingface.co/litert-community)
- [SPEC-004](SPEC-004.md) — Transformer Architecture do OpenCode
- [SPEC-934](SPEC-934.md) — Padrão de Skills do Ecossistema
