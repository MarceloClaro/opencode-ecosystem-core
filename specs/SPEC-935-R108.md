# SPEC-935-R108 — Fusão do Pipeline Científico R101-R105 no Orquestrador MarceloClaro

## Objetivo

Fundir nativamente o pipeline agentivo científico R101-R105 (EvoSci → Deep
Research → Peer Review → Revision → Paper Composer) ao orquestrador
`MarceloClaroOrchestrator`, substituindo o encadeamento ad hoc que existia
apenas em `webapp/pipeline_helpers.py`, e passar a usar a infraestrutura
metacognitiva real do MCI (calibração de confiança, gate de exportação real
entre estágios, avaliação SPEC-920 sobre traços reais de execução) em vez de
heurísticas isoladas e desconectadas por módulo.

## Motivação

A auditoria (R107 e investigação subsequente) revelou que `agentic_science_v2`
vivia como um silo completamente isolado do MCI:

- Zero uso do MetaBus/Reflexion/Trust Engine em qualquer um dos 5
  orquestradores internos (R101-R105).
- Nenhum logging e nenhum tratamento de erro nos entrypoints públicos.
- Cada estágio calculava sua própria "confiança"/"score" com heurísticas
  isoladas, nunca calibradas, enquanto o ecossistema já possui
  `mci/confidence_calibrator.py` (Brier Score + ECE + abstenção) e
  `mci/metacognitive_evaluator.py` (SPEC-920, 6 dimensões) sem uso real —
  o SPEC-920 só avaliava um benchmark sintético estático, nunca traços reais
  de produção.
- Mesmo quando o R103 reprovava `export_gate_passed`, o pipeline continuava
  cegamente para R104d/R105 — nenhum gate real entre estágios.
- Contrato R103→R104d era corrigido via adaptador externo em
  `webapp/pipeline_helpers.py` (`_coerce_review_package`), não na origem.

## Escopo

### Em escopo

- `agentic_science_v2/review_agent.py`: `ReviewPackage.to_revision_contract()`
  (contrato R103→R104d resolvido na origem)
- `agentic_science_v2/deep_research.py`, `review_agent.py`,
  `revision_agent.py`, `paper_composer.py`: logging + tratamento de erro
  estruturado nos entrypoints públicos
- `agentic_science_v2/revision_agent.py`: rollback automático quando
  `verify_integrity()` reprova
- `agentic_science_v2/paper_composer.py`: correção de bug pré-existente em
  `write_introduction` (slice sobre dict de entidades do Evidence Graph)
- `marceloclaro/orchestrator.py`: novo método
  `scientific_discovery_pipeline()` — gate real, calibração Brier via
  `mci.confidence_calibrator`, avaliação SPEC-920 via
  `mci.metacognitive_evaluator` sobre traços reais do run, eventos no
  MetaBus, aprendizado no Trust Engine
- `webapp/pipeline_helpers.py::run_full_academic_pipeline`: passa a
  delegar ao orquestrador, preservando o contrato de UI existente
- Testes de regressão e integração

### Fora de escopo (registrado para ciclos futuros)

- Unificação das duas implementações divergentes de `EvidenceGraph`
  (`mci/evidence_graph.py` vs `agentic_science_v2/evidence_graph.py`)
- Reescrita do `ReflexionEngine` global para reflexão via LLM real
  (atualmente template determinístico — usado por dezenas de subsistemas)
- Conexão de `integrations/model_router.py` ao `MarceloClaroOrchestrator`
  (permanece desconectado, como já estava)

## Critérios de Aceitação

1. `scientific_discovery_pipeline()` executa R101→R102→R103→(R104d→R105
   condicionalmente) e retorna um relatório único com `status`, `stages`,
   `gate_decision`, `calibrated_confidences` e `metacognitive_report`
2. Quando `strict_gates=True` e o R103 reprova `export_gate_passed`, o
   pipeline **para antes** de R104d/R105 (`status: "blocked"`)
3. Quando `strict_gates=False`, o pipeline continua mesmo com gate
   reprovado (comportamento configurável, não removido)
4. Cada estágio tem sua confiança bruta calibrada via
   `mci.confidence_calibrator.calibrate_confidence`; estágios que falham
   produzem `should_abstain=True` (calibração < 0.20)
5. `metacognitive_report` é computado por
   `mci.metacognitive_evaluator.MetacognitiveEvaluator` sobre os traços
   **reais** deste run (não o benchmark sintético estático), e nunca
   classifica o tier como `metacognitive_superhuman_verified` sem
   validação externa explícita (política já existente, preservada)
6. Exceção não tratada em qualquer estágio retorna `status: "error"`
   estruturado em vez de propagar o crash
7. `OrchestratorRevision.run()` reverte automaticamente (rollback) quando
   `verify_integrity()` reprova, e rebaixa revisões `applied` para
   `rejected` no relatório
8. `ReviewPackage.to_revision_contract()` produz diretamente o contrato
   que `ReviewAnalyzer.extract_claims()` espera, sem adaptador externo
9. `webapp/pipeline_helpers.py::run_full_academic_pipeline` preserva todas
   as chaves consumidas por `webapp/app.py` (`evosci`, `deep_research`,
   `peer_review`, `manuscript_revision`, `paper`, `timeline`), incluindo o
   caso `pipeline_result: "blocked"`
10. Suíte principal permanece verde, sem regressões nos testes R101-R107

## Estratégia de Verificação

- `tests/test_r108_marceloclaro_scientific_fusion.py` (10 testes): gate
  real, calibração/abstenção, avaliação sobre traços reais, tratamento de
  erro, rollback automático, contrato de revisão, não-regressão do webapp
- Execução manual ponta-a-ponta de
  `MarceloClaroOrchestrator().scientific_discovery_pipeline(...)` com
  `strict_gates=True` e `strict_gates=False`
- `python3 -m pytest tests/ -q --tb=short`

## Riscos

- O gate real do R103 (`min_traceability=0.5`) reprova com frequência
  dado o rubric heurístico atual — isso é o comportamento **correto e
  pretendido** (o pipeline agora respeita o gate em vez de ignorá-lo), mas
  muda visivelmente o resultado padrão do botão "Executar Pipeline
  Completo" na webapp, que passa a retornar `blocked` com mais frequência
  do que antes. `strict_gates=False` permanece disponível para quem
  preferir o comportamento permissivo anterior.
- A calibração de confiança usa `context["reproducibility_score"]` como
  sinal principal por estágio (mapeamento honesto, não uma alegação
  estatística formal como p-valor/effect size, que não existem nas
  heurísticas 0-1 do R101-R105).

## Entrega Esperada

Orquestração única e genuinamente metacognitiva do pipeline científico,
sem alegação de superioridade sobre nenhum sistema externo — segue a
mesma política anti-overclaim já codificada em
`classify_metacognitive_tier` (SPEC-920).
