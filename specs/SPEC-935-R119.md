# SPEC-935-R119 — Templates literários/de ficção + limpeza de resíduos do catálogo

## Objetivo

1. Remover os 37 diretórios numerados legados de `templates/` (`01_` a `36_`),
   remanescentes de antes da reorganização documentada em `templates/README.md`
   (SPEC-916), que duplicavam integralmente os templates já categorizados.
2. Adicionar ao catálogo os dois primeiros templates **literários/de ficção**
   (`romance-literario` e `contos-poesia`) — até aqui, todo template de
   "livro" do catálogo era acadêmico ou técnico (Victoria Regia, UnB Editora,
   Springer, book-simple).
3. Integrar os novos templates ao pipeline de produção
   (`publishing/production.py`) e ao conversor `pdf2latex`
   (`pdf2latex/template_integrator.py`).

## Motivação

Pedido do usuário: "renove os templates de artigos a livros literários".
Levantamento confirmou que os templates de "livro" existentes eram todos
acadêmicos/técnicos — não havia opção para romance, novela ou coletânea de
contos/poesia dentro do catálogo (o único precedente, o livro "Tritemo", foi
produzido por um script avulso fora do catálogo de templates).

Em paralelo, o usuário pediu "faça a limpeza de resíduos": os 37 diretórios
numerados em `templates/` eram cópias renomeadas dos templates já
organizados em `academic-br/international/books/cv/projects`, confirmadas
por diff e por hash SHA-256 de todos os arquivos.

Por fim, o usuário pediu para transformar PDFs reais (`MOLAMBUDOS.pdf`,
seu próprio romance, e `main-dark.pdf`) em templates LaTeX usando o
conversor `pdf2latex` já existente no repositório (SPEC-1000, em
desenvolvimento paralelo) — o que expôs e permitiu corrigir dois bugs
reais de integração (ver "Correções colaterais" abaixo).

## Escopo

### Em escopo
- Remoção de `templates/01_*` a `templates/36_*` (37 diretórios), preservando
  antes os poucos arquivos com conteúdo único encontrados via hash:
  - `templates/projects/livro-volume2/.opencode_integration.json`,
    `.sdd_v2_specs.md`, `.tdd_validate_v2.py` (metadados SDD/TDD do
    projeto, só existiam em `03_Livro_Volume2`)
  - `templates/abntex2/abntex2-modelo-references-bibdesk-example.bib`
    (só existia em `12_Template_ABNTEX2`)
- Dois novos templates em `templates/books/`:
  - `romance-literario/` — romance/narrativa longa (dropcap, quebra de
    cena, sem aparato acadêmico, formato trade paperback 5.5×8.5in)
  - `contos-poesia/` — coletânea de contos e poemas (capítulos
    autocontidos, ambiente `poema` próprio)
- Symlinks de compatibilidade `publishing/templates/livro/romance` e
  `publishing/templates/livro/contos`, seguindo o padrão já usado por
  `book`/`victoria_regia`
- `TEMPLATE_MAIN` em `publishing/production.py`: novas chaves
  `livro-romance` e `livro-contos`
- Atualização de `templates/README.md` (catálogo, compatibilidade
  retroativa, estatísticas)
- Testes de regressão em `tests/test_r119_templates_literarios.py`

### Correções colaterais (feitas fora de `publishing/`, no módulo
`pdf2latex/` — trabalho em progresso de outra frente, SPEC-1000, não
commitado neste ciclo por já ter mudanças pendentes não relacionadas;
mudanças aplicadas em disco e verificadas, mas cabe à frente do SPEC-1000
commitá-las junto do restante)
- `pdf2latex/template_integrator.py`: registrados `romance-literario` e
  `contos-poesia` em `TEMPLATE_ALIASES`/`TEMPLATE_CLASS`/`TEMPLATE_BIBSTYLE`;
  adicionado `misc` à lista de subdiretórios copiados (necessário para
  `misc/opcoes.sty` chegar ao projeto gerado)
- `pdf2latex/latex_generator.py::_get_figures_for_section()`: a legenda
  `\caption{...}` era inserida sem escapar caracteres especiais LaTeX
  (`_escape_latex()` já existia mas não era chamado ali) — qualquer PDF
  com 2+ imagens cujo nome de legenda contivesse `_` (ex.: `figura_1`)
  quebrava a compilação com "Missing $ inserted". Corrigido chamando
  `self._escape_latex(caption)`.
- `templates/books/romance-literario/misc/opcoes.sty` e
  `templates/books/contos-poesia/misc/opcoes.sty`: carregamento de
  `babel` e `geometry` tornado seguro para reentrada (guardas
  `\@ifpackageloaded`/uso do comando `\geometry{}` em vez de reabrir
  `\usepackage[...]{geometry}`), pois um main.tex gerado pelo pdf2latex
  já carrega esses pacotes com opções próprias — carregar de novo com
  opções diferentes gerava "Option clash" fatal.
- `templates/books/contos-poesia/`: removida dependência do pacote
  externo `verse` (ausente na instalação TeX Live testada) — substituído
  por um ambiente `poema` local baseado em `quote`.

### Fora de escopo
- Fixar a qualidade da extração de texto/estrutura do `pdf2latex` em si
  (heurísticas de detecção de capítulo, rótulos duplicados entre
  capítulos) — pertence à frente do SPEC-1000, não a este ciclo.
- Mover ou versionar os PDFs de origem e os projetos LaTeX gerados a
  partir deles (`templates/livro/MOLAMBUDOS.pdf`, `main-dark.pdf`,
  `templates/livro/molambudos/`, `templates/livro/ia-em-odontologia/`) —
  são conteúdo do usuário, não parte do catálogo reutilizável de
  templates; ficam em disco, fora do commit deste ciclo.

## Critérios de Aceitação

1. `ls templates/ | grep -E "^[0-9]{2}_"` retorna vazio
2. `templates/books/romance-literario/main.tex` e
   `templates/books/contos-poesia/main.tex` existem e compilam sem erro
   fatal via `latexmk -pdf`
3. `TEMPLATE_MAIN` inclui `livro-romance` e `livro-contos` apontando para
   diretórios existentes
4. `ScientificProduction.prepare_latex()` com `template="livro-romance"`
   ou `"livro-contos"` copia a árvore inteira do template, incluindo
   `misc/opcoes.sty`
5. Suíte de testes completa permanece verde (exceto a falha intermitente
   pré-existente e não relacionada `test_failed_task_lowers_confidence`)

## Estratégia de Verificação

- `tests/test_r119_templates_literarios.py` (9 testes): existência dos
  arquivos de template, integração com `TEMPLATE_MAIN`/`list_templates()`,
  symlinks de compatibilidade, `prepare_latex()` copiando a árvore
- Compilação real via `latexmk -pdf` do projeto gerado por
  `pdf2latex templates/livro/MOLAMBUDOS.pdf --template romance-literario`
  (341 páginas de entrada) — PDF de saída gerado com sucesso após as
  correções de `babel`/`geometry`/escape de legenda
- `python3 -m pytest tests/ -q` completo
